# Agent/tooling scratch — local to whoever is running the tools, never part of
# the site build. The site itself lives in site/ and has its own .gitignore.
.superpowers/
.claude-flow/
.swarm/
ruvector.db

# Local Claude Code settings — settings.json's hooks reference machine-specific
# %USERPROFILE% paths, so this is per-machine, not shared. CLAUDE.md and
# .mcp.json ARE committed: those are the shared, portable half of the config.
.claude/
